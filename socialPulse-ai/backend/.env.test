# SocialPulse AI — Environment Variable Validation
# This file is intentionally minimal — full variables are in .env.example
# Run: python -c "from app.core.config import settings; print('Config OK')"
